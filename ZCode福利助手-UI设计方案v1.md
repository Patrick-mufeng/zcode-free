# ZCode 福利自动领取助手 — UI 设计方案 v1.0

| 项 | 内容 |
|---|---|
| 版本 | v1.1 / 2026-09-10(视觉供应商定为 DeepSeek) |
| 形态 | 纯 Web 面板:Python 后端 + 本地服务,浏览器打开(无原生窗口/托盘) |
| 关联文档 | 《ZCode福利助手-技术方案v1.md》 |
| 使用对象 | 本人(单机、单账号) |

## 1. 设计目标与原则

- **一个面板管全部**:场次配置、启停、日志、截图回放、识别参数,都在一个窗口里完成,不开第二个程序。
- **自动为主,看得见**:默认无人值守,但任何一次自动操作都要能在事后"回放"——每一步的状态、截图、判定理由都可见。
- **状态一眼可读**:全界面只用一套语义色(运行/成功/失败/重试/空闲),用户不读日志也能知道当前情况。
- **离线、零构建**:HTML/CSS/JS 纯静态(不引 CDN、不需要 node 构建),双击 `run.bat` 起服务并自动打开面板。

## 2. 形态与技术

- **纯 Web 面板**:Python 进程在 `127.0.0.1:8760` 起本地服务(仅回环,不对外暴露),启动时自动用默认浏览器打开;`--no-open` 只起服务不弹浏览器。无原生窗口、无托盘。
- 前后端通信(实现版):
  - **JS → Python**:`POST /api {method, args}` JSON-RPC(方法名见 §10 方法表)。
  - **Python → JS**:`GET /events` SSE 事件流(`state_change` / `exec_start` / `step_update` / `attempt_update` / `log_line` / `session_end` / `need_manual`),前端订阅后局部刷新,不用轮询。
- 页面为纯静态 HTML/CSS/JS(离线可用、无构建);关闭浏览器标签不影响后端与自动领取;数据层直读 `data/config.yaml`,UI 即配置器。
- 执行领取动作时,面板**建议最小化**(避免自身窗口遮住 ZCode 影响截图/点击),UI 在执行开始时顶部横幅提示一次。

## 3. 使用场景与主流程

```
场景A 日常无人值守:开机自启 → 浏览器标签可关可留(不影响后台) → 到点自动领取 → 成功静默/失败通知
场景B 手动试领:     点顶栏 [试领一次] → 面板切到"运行日志"页实时看步骤与截图
场景C 首次配置:     设置ZCode路径 → 填API Key → [测试识别] → 添加场次 → 开总开关
场景D 事后核查:     运行日志页 → 点某次尝试缩略图 → 大图看当时画面与判定结果
```

## 4. 信息架构

```
ZCode福利助手(单窗口,6 个页面)
├─ ① 概览 Dashboard     运行总况 / 手动测试(试领·演练) / 上手检查 / 最近记录
├─ ② 流程 Flow          整体流程图(执行时实时高亮)+ 识别逻辑 + 判定规则 + 坐标换算
├─ ③ 场次表 Schedule    每天固定场次的增删改、启用、未来 7 天预览
├─ ④ 识别 Vision        视觉模型连接与识别参数、词表管理、测试识别
├─ ⑤ 运行日志 Logs      实时日志流 + 按场次的尝试卡片(截图回放)
└─ ⑥ 设置 Settings     ZCode 路径/窗口、重试节奏、通知、数据清理、危险区
```

## 5. 全局框架(所有页共用)

采用「左侧导航栏 + 顶部状态栏」的控制台骨架:

```
┌──────────────┬────────────────────────────────────────────────────────┐
│ ◈ ZCode      │ ● 空闲            下一场 10:00   (还有 02:13:09)         │
│   福利助手    │ 自动领取待命                       [开关 ON] [▶ 试领一次] │ ← 顶栏(常驻)
│              ├────────────────────────────────────────────────────────┤
│ ▸ 概览       │                                                        │
│ ▸ 场次表     │           内容区(见 §6,各页统一 16px 间距)              │
│ ▸ 识别       │                                                        │
│ ▸ 运行日志   │                                                        │
│ ▸ 设置       │                                                        │
│              │                                                        │
│ ● 127.0.0.1  │                                                        │
│ v1.1 · 本地服务│                                                       │
└──────────────┴────────────────────────────────────────────────────────┘
```

- **侧栏**:品牌区(渐变 Logo + 名称/副标题)、5 个导航项(SVG 图标 + 文字,选中项由滑动光条指示)、底部 SSE 连接状态灯(连通绿 / 断开黄)与版本号;≤1120px 自动收成 74px 图标栏。
- **顶栏**:状态灯 + 双行文案、下一场时间与倒计时、总开关、主按钮,全部常驻。

顶栏要素说明:

| 元素 | 规则 |
|---|---|
| 状态点 | `空闲`(灰)/ `等待中`(蓝,距下场<10min)/ `执行中`(蓝,呼吸动画)/ `成功`(绿,短暂闪烁后回空闲)/ `失败·需人工`(红,保持到人工点掉) |
| 主开关 | 总电源。OFF 时调度不触发,但"试领一次"仍可用 |
| 试领一次 | 手动触发单次全流程(不入调度);**执行中禁用**;执行时标签条自动跳到"运行日志"页 |
| 下一场倒计时 | `HH:MM · DD:HH:MM:SS`,来自调度器 next_slot();无场次时显示"未配置场次"并点击跳场次表页 |

## 6. 页面详细设计

### 6.1 ① 概览 Dashboard

```
┌───────────────────────────────────────────────────────────────┐
│ ┌──────────────┐ ┌──────────────┐ ┌──────────────┐ ┌────────┐ │
│ │ 下场时间      │ │ 今日场次      │ │ 今日结果      │ │        │ │
│ │ 今天 10:00   │ │ 3 场 / 启用3  │ │ ✓2  ✗1  -0  │ │ 卡片区 │ │
│ │ 距现在 07:23 │ │ 明日 3 场     │ │ 最近一次:成功 │ │        │ │
│ └──────────────┘ └──────────────┘ └──────────────┘ └────────┘ │
│ ┌─ 最近 3 次记录 ──────────────────────────────────────────┐  │
│ │ 今天 10:00  成功   第2次重试后领取成功   10:00:03  [查看] │  │
│ │ 昨天 16:00  失败   5次重试均未成功       16:00:41  [查看] │  │
│ │ 昨天 10:00  成功   首次点击即成功         10:00:02  [查看] │  │
│ └──────────────────────────────────────────────────────────┘  │
│ 执行中:                                    ┌─────────────┐   │
│ [打开客户端✓][定位按钮✓][点击…][校验]       │ 本次截图     │   │
│  步骤条(高亮当前步,失败步标红)              │ (实时更新)   │   │
└──────────────────────────────────────────────────────────────┘
```

- 步骤条只在执行/试领时出现(5 步:打开客户端 → 定位按钮 → 点击 → 校验 → 收尾),每步旁小状态点;失败时该步红点并展开"重试第 n 次"徽标。
- 执行中的实时截图小窗:每次尝试抓拍后更新缩略图,让用户肉眼确认机器"没点错地方"。
- **手动测试区**(运行中禁用):「试领一次(完整流程)」= 立即跑完整领取;「演练一次(不点击、不关客户端)」= 同样先置顶窗口再截图识别,但不动鼠标、不关客户端(演练也置顶,否则被遮挡时会截到别的窗口造成假阴性)。两者都不进调度,执行过程与结果照常写入「运行日志」。
- **流程页联动**:执行时「流程」页的流程图会实时高亮当前步骤,结束后显示本次实际走过的路径(未执行到的节点保持中性),并高亮对应出口分支。
- 上手检查清单:三步(填 Key / 加场次 / 开总开关)按真实状态打绿勾并显示"已完成",全部完成后面板自动收起。
- 空状态:今日无场次 → 卡片区显示引导条"先到 ② 场次表 添加每日场次"。

### 6.2 ② 场次表 Schedule

```
┌───────────────────────────────────────────────────────────────┐
│ 每日固定场次                          [+ 添加场次]            │
│ ┌───────────────────────────────────────────────────────────┐ │
│ │  启用   时间    尝试次数  重试间隔    最近结果     操作      │ │
│ │  ☑    10:00    (默认5)   (默认5s)   今天✓          ✎  🗑   │ │
│ │  ☑    16:00    (默认5)   (默认5s)   昨天✓          ✎  🗑   │ │
│ │  ☐    22:00    (默认5)   (默认5s)   —              ✎  🗑   │ │
│ └───────────────────────────────────────────────────────────┘ │
│ 未来 7 天预览                                                 │
│ 今天 ●10:00 ✓  ✗16:00   · 09-11 ●10:00 ●16:00 ●22:00   …    │
└───────────────────────────────────────────────────────────────┘
```

- 添加/编辑弹层:时间 `HH:MM`(原生 time input)、启用开关;尝试次数/重试间隔留"继承全局默认"或单独覆盖(默认值来自 `retry.*`,见技术方案 §8)。
- 编辑保存 → 调度器热更新,Toast"场次表已更新"。
- 预览时间线:用本机时区;可点击某天某场跳到该场日志(收拢筛选)。
- 空状态:无任何场次时列表区大引导插画+"添加我的第一场"主按钮。

### 6.3 ③ 识别 Vision

```
┌───────────────────────────────────────────────────────────────┐
│ 模型连接                                                      │
│ Provider   [DeepSeek(默认)▾]  Key 申请:platform.deepseek.com  │
│ base_url   [https://api.deepseek.com                  ]       │
│ API Key    [••••••••••••••••••••••••              ]  [显示]   │
│ Model      [deepseek-v4-flash-vision-exp▾]                    │
│ Temperature [0.2 ────]   detail [auto▾] (low 更快更省)         │
│                                    [连接与额度测试]            │
│ ┌─ 识别参数 ──────────────────────────────────────────────┐  │
│ │ 定位置信度阈值 [0.70 ──────────]  (低于此值不点击,报unknown)│  │
│ │ 点击后等待校验 [2.5 s ▾]  视觉超时 [20 s]                 │  │
│ │ 界面渲染等待上限 [20 s]  结果弹窗渲染上限 [8 s]              │  │
│ └──────────────────────────────────────────────────────────┘  │
│ 判定词表(点击后文案命中即成功/已领,chip 可增删)                 │
│ 成功词 [领取成功][开始体验][奖励到账][已到账][领取奖励][获得][+]           │
│ 已领词 [已领取][今日已领][明天再来][已参加][次数已用完]                  │
│ 失败词 [领取失败][知道了]                                        │
│ 识别提示词(定位/校验两段)  [展开只读查看,高级可编辑]            │
│ ┌─ 测试识别 ──────────────────────────────────────────────┐  │
│ │ [选择截图…] 或 [截取当前ZCode窗口]  →  识别结果JSON展示  │  │
│ └──────────────────────────────────────────────────────────┘  │
└───────────────────────────────────────────────────────────────┘
```

- API Key 仅存本机 config.yaml,前端输入框 `type=password` + 显示切换。
- 后端发送前会把截图裁剪成 ZCode 窗口区域,并要求模型返回**归一化坐标**——DeepSeek 视觉模型内部会把图缩到约 800×800 等效,裁剪 + 归一化是保证按钮定位精度的关键(见技术方案 §3.1/§6.4);测试识别的按钮框叠加即由归一化坐标换算而来。
- "连接与额度测试":调一次 1×1 像素图识别,返回耗时/可用;失败给出常见原因(网络/Key 错/余额)。
- "测试识别"是**新用户上手的关键路径**:截一张当前福利页 → 展示返回 JSON(场景/按钮框/置信度)→ 图上叠加画出按钮框,若框画错立即知道要调什么(换截图角度或改词)。
- 识别提示词只读展示为主(给用户安全感,"知道它在让模型做什么"),高级区可展开编辑。

### 6.4 ④ 运行日志 Logs(核心回放页)

```
┌───────────────────────────────────────────────────────────────┐
│ 筛选: [本场回放▾][全部] [只看失败]  自动滚动 ☑  搜索 [       ] │
├───────────────┬───────────────────────────────────────────────┤
│ 场次列表(左)   │  尝试详情(右,选中某场时)                        │
│ 今天 10:00 ✓  │  ┌─────────────────────────────────────────┐  │
│   第2次成功    │  │ 10:00:00.1 打开客户端            ✓ 0.8s  │  │
│ 昨天 16:00 ✗  │  │ 10:00:01.2 窗口就绪(1240x860)    ✓      │  │
│   5次全失败    │  │ 10:00:02.0 ── 第 1 次尝试 ──────────     │  │
│ 昨天 10:00 ✓  │  │ 10:00:02.3 截图 → 视觉定位        ✓      │  │
│               │  │   命中[领取] 置信0.92                    │  │
│               │  │   [缩略图:定位图]  [缩略图:点击后校验图]    │  │
│               │  │ 10:00:03.9 校验:未成功(无成功文案)        │  │
│               │  │ 10:00:04.0 关闭客户端 → 5s 后重试         │  │
│               │  │ 10:00:09.5 ── 第 2 次尝试 ── 领取成功 ✓   │  │
│               │  │ [缩略图] [缩略图] 判定:命中[领取成功]      │  │
│               │  └─────────────────────────────────────────┘  │
└───────────────┴───────────────────────────────────────────────┘
  日志流(底部,实时追加,级别配色):  [14:00:00] 调度器|10:00场触发 …
```

- 左列每场一条,徽标 ✓/✗ + 简况;右侧回放**按时间轴逐行**:每行 = 一次动作,缩略图就近摆放。
- 缩略图点击 → Lightbox 大图 + 元信息(文件路径、尺寸、所属 attempt);大图可"框出模型判定的按钮位置"(定位图用红框叠加,来自视觉返回的 bbox)。
- 日志行五级配色:INFO 默认 / 动作蓝 / 成功绿 / 失败红 / 异常黄;支持按级别过滤与关键词搜索。
- 实时执行时右侧自动切到"进行中场次"并跟随滚动;执行结束跟随自动解除。

### 6.5 ⑤ 设置 Settings

```
┌───────────────────────────────────────────────────────────────┐
│ 程序与ZCode                                                   │
│ ZCode 程序路径 [C:\…\ZCode.exe        ]  [自动探测]            │
│ 窗口标题匹配  [ZCode]     每次复位窗口位置 ☑ 位置[40,40,1240,860]│
│ 开机自启 ☐(写入启动项)                                        │
│ 通知                                                          │
│ 桌面通知 ☑  [执行中提示 ☐]  [失败必发 ✓]  Webhook [URL…] ☐      │
│ 系统                                                          │
│ 场次执行期防休眠 ☑   截图/日志保留 [7天▾]                       │
│ 危险区: [清除全部截图] [清空日志] [重置配置为默认]   (均二次确认) │
└───────────────────────────────────────────────────────────────┘
```

- 一切修改**即时写盘**,不设"保存"按钮(每项变更即持久化 + Toast),减少遗忘保存的出错点。
- "自动探测"复用 M1 探测逻辑,找到即回填路径。

### 6.6 ② 流程 Flow(可视化说明页)

给使用者解释"机器到底怎么判断的",同时充当运行时仪表盘:

1. **整体流程图**:7 个节点(触发 → 打开客户端 → 截图 → 视觉定位① → 点击 → 校验② → 收尾)+ 3 条出口分支(成功弹窗、失败弹窗/未成功重试、需人工)。**执行时实时高亮**:已完成节点打绿勾、当前节点发光上浮、失败标红,分支卡片闪一下表示本次走了哪条路;右上角胶囊显示"执行中 · 第 n 次尝试 · 当前步骤"。
2. **识别逻辑**:4 张阶段卡(截图 / 定位(模型①) / 点击 / 校验(模型②)),每张写清输入、输出字段与关键约束(如归一化坐标、结构化输出三重保障)。
3. **判定规则表**:模型只给"观察",规则做"决策"——把 8 种情况(场景异常 / 已领 / 可点 / 未找到 / 成功弹窗 / 失败弹窗 / 校验已领 / 无变化)与对应动作列成表格。
4. **坐标换算**:用三个盒子展示"窗口矩形 + 归一化中心点 = 屏幕坐标"的算式,解释为什么用 0~1 归一化(模型内部缩放不影响比例)。

## 7. 视觉与组件规范

### 7.1 主题与色彩(深色为主,「Aurora Console」电影感方向)

| CSS 变量 | 值 | 用途 |
|---|---|---|
| `--bg` | `#060B18` | 页面底色(不用纯黑,避免 OLED 拖影) |
| `--surface` | `rgba(148,163,184,.055)` | 面板/卡片玻璃底 |
| `--surface-2` | `rgba(148,163,184,.095)` | 次级容器、悬浮层 |
| `--border` / `--border-strong` | `rgba(148,163,184,.14)` / `.26` | 细线分隔 / 强调边框 |
| `--text-1 / 2 / 3` | `#E9EEF9` / `#A6B3CE` / `#7C8AA8` | 主/次/弱文本(三级均满足深色对比度要求) |
| `--brand` → `--brand-2` | `#6366F1 → #8B5CF6`(配 `#22D3EE` 收尾) | 品牌渐变:主按钮、选中态、Logo |
| `--ok` / `--warn` / `--err` / `--info` | `#34D399` / `#FBBF24` / `#F87171` / `#60A5FA` | 成功 / 重试·等待 / 失败·危险 / 进行中 |

**氛围层**:固定背景上叠 3 个模糊光斑(Indigo / Cyan / Violet,blur 90px,缓慢漂移)+ 极淡噪点,形成纵深;面板用 `backdrop-filter: blur(12px)` 玻璃拟态。

**语义色唯一约定**(全局不得混用):绿=成功,红=失败/危险,黄=重试/待观察,蓝=进行中/信息。品牌紫只用于"操作与选中"。

### 7.2 字体、字号、间距
- 字体栈:`"Inter", "Segoe UI Variable Text", system-ui, "Microsoft YaHei UI", sans-serif`(Inter 未安装时自动回退系统字体,不依赖 CDN);日志/ID/时间追加 `"Cascadia Mono", Consolas, monospace` + `tabular-nums`。
- 字号:统计值 26/标题 13.5/正文 13/辅助 11.5–12/日志 11.5 等宽;行高 1.6。
- 间距:8pt 栅格;卡片圆角 16px、控件 9–12px;面板内边距 16–18px;主内容最大宽 1360px。
- 数字一律 `font-variant-numeric: tabular-nums`,避免跳动。

### 7.3 组件清单

| 组件 | 规范要点 |
|---|---|
| 侧栏导航 | 左侧 236px(窄屏收成 74px 图标栏);选中项由一条 **GSAP 驱动的高亮光条**滑动指示,而非生硬换背景 |
| 主开关 | 48×27 胶囊,ON 为品牌渐变 + 外发光;执行中置灰禁点 |
| 状态灯 | 10px 圆点 + 双行文案(状态/副说明);执行中呼吸外环,成功闪两下,失败持续脉冲 |
| 主按钮 | 品牌渐变 + 顶部 1px 高光 + 悬浮上浮 1px + 按下缩放 0.98 |
| 统计卡片 | 顶部 1px 渐变细线(品牌→青),悬浮上浮 2px,数值变化时数字滚动 |
| 步骤时间线 | 5 步胶囊 + 渐隐连接线;完成=绿实心钩、当前=蓝呼吸、失败=红叉 |
| 上手检查 | 三步待办;完成项=绿实心勾 + "已完成"胶囊 + 标题删除线,全部完成后面板自动收起 |
| 徽标 chip | 高 20px 胶囊;底 14–16% 前景色 + 38% 描边(如成功:底 `#34D39924` 字 `#6EE7B7`) |
| Toast | 右上角弹性入场(back.out),3.2s 自动消失;左侧 3px 语义色条 |
| 弹层 | 遮罩 70% 深色 + 模糊;卡片从 0.97 缩放进入,关闭反向淡出 |
| Lightbox | 深色遮罩 + 模糊,点击任意处或 Esc 关闭 |
| 空状态 | CSS 绘制的虚线圆 + 说明文字,禁止空白页;日志区空时显示"暂无日志输出" |
| 图标 | 统一内联 SVG(Lucide 风格,1.8 描边,currentColor),**不使用 emoji 作为图标** |

### 7.4 动效规范(GSAP 3.13,已本地化)

- **库**:`ui/assets/js/vendor/gsap.min.js`(离线,无 CDN);动效集中在 `ui/assets/js/motion.js`,通过 MutationObserver 观察 DOM,**不侵入业务逻辑**。
- **时长/曲线**:统一 `expo.out`;入场 0.5–0.55s、微交互 0.15–0.24s、列表 stagger 0.04–0.07s;退场比入场快。
- **只用 transform / opacity**,不动 width/height/top/left,避免重排与 CLS。
- **具体动效**:开场序列(品牌→导航→顶栏→侧栏页脚→首屏卡片依次浮入);切页时内容区 stagger 浮入;导航光条滑动;状态灯呼吸/闪烁;步骤圆点状态弹跳;实时截图交叉淡入;Toast 弹性入场;光斑缓慢漂移;数字滚动。
- **无障碍与降级**:尊重 `prefers-reduced-motion`;页面处于后台标签页时不播放入场动画(避免 rAF 暂停导致元素停在透明态);GSAP 缺失时自动静态呈现;支持 `?motion=0` 手动关闭动效。

## 8. 关键交互细节

1. **执行中锁操作**:主开关、"试领一次"、场次表编辑在执行期间禁用(灰色),执行结束自动解锁——保证不会在跑一半时改配置。
2. **试领的观看体验**:点"试领一次"→ 自动切到"运行日志"页 → 步骤逐行走时间轴 + 截图实时出现;若失败进入重试,右侧卡片显示"第 n 次尝试"横幅。
3. **执行提示横幅**:执行/试领开始时顶部滑出黄条:"执行中:请勿让浏览器窗口遮挡 ZCode(可最小化本窗口,自动化会把 ZCode 切到前台)",执行结束或失败时自动让位给结果提示。
4. **失败态停留**:RECORD_FAIL 后顶栏状态点保持红色"失败·需人工"直到用户点击;任何"需人工"通知同步出现红色横幅,点击横幅跳日志定位现场。
5. **配置即存即生效**:任何修改 → 写 config.yaml → 调度器热重载;失败则 Toast 报错并回滚该项显示。
6. **首次引导(向导式横幅序列)**,按缺什么显什么:缺 ZCode 路径 → 缺 API Key → 没场次 → 没开主开关;每项横幅带 [去配置] 直达按钮,配完一条消一条,全部就绪才显示"就绪"。

## 9. 前端实现约定

```
ui/
├── index.html            # 单页;5 页签通过 JS 切换显示,不做路由跳转
└── assets/
    ├── css/app.css       # tokens(§7.1) + 组件 + 布局
    └── js/
        ├── app.js        # 页面切换、顶栏状态、主开关
        ├── api.js        # /api RPC 封装(§10),统一 loading/error
        ├── events.js     # Python 事件订阅 → DOM 局部更新(日志行/截图/状态)
        ├── pages/dashboard.js  / schedule.js  / vision.js  / logs.js  / settings.js
        └── vendor/       # 如选型需要,本地存放第三方库(离线可用)
```

- 纯原生 JS + 少量 DOM 模板字符串即可,不引框架;如需日期处理等再本地 vendor 一个库。
- 与后端仅通过 §10 的 `api`(POST /api)与 `event`(SSE)两个通道通信,不依赖任何桌面窗口框架。

## 10. 后端方法表(POST /api,Python 侧实现)

| 方法 | 入参 → 出参 | 说明 |
|---|---|---|
| `get_state()` | → `{running, currentSlot, nextSlot, todayStats, lastRecords[]}` | 概览/顶栏全量状态 |
| `set_master(bool)` | → `{ok}` | 主开关 |
| `run_once({dry})` | → `{ok}` | 手动跑一次;`dry=true` 为演练(不点击、不关客户端) |
| `get_schedule()` / `save_schedule(list)` | → `{ok, slots}` | 场次表(写盘+热重载) |
| `get_settings()` / `save_settings(patch)` | → `{ok, settings}` | 设置读写,局部 patch |
| `detect_zcode()` | → `{ok, path}` | 自动探测 exe |
| `get_vision_config()` / `save_vision_config(patch)` | → `{ok}` | 识别页 |
| `test_vision_connection()` | → `{ok, ms, message}` | 额度/连通测试 |
| `test_locate(filePath?)` | → `{ok, result, imageB64}` | 测试识别:传截图或截当前窗口 |
| `get_logs(filter)` | → `{items[]}` | 日志查询(分页/搜索) |
| `list_sessions()` / `get_session_detail(id)` | → 场次列表 / 该场时间轴+截图 | 日志回放数据 |
| `get_shot(path)` | → `{imageB64}` | 大图查看 |
| `cleanup(action)` | → `{ok}` | 清截图/清日志/重置配置(前端先二次确认) |
| `quit_app()` | → `{ok}` | 退出整个程序(会中断正在执行的领取) |

## 11. Python → 前端事件表

| 事件 | 载荷 | 触发时机 |
|---|---|---|
| `state_change` | 同 get_state | 任何状态变化(顶栏/概览刷新) |
| `exec_start` | `{sessionId, trigger}` | 场次触发或试领开始(切页/横幅/步骤条) |
| `step_update` | `{sessionId, step, status, detail}` | 步骤条前进/失败 |
| `attempt_update` | `{sessionId, attempt, status, images[]}` | 每次尝试的截图与结果 |
| `log_line` | `{ts, level, message}` | 实时日志追加 |
| `need_manual` | `{reason, sessionId}` | 需人工类异常(红横幅) |

## 12. 验收清单(UI)

- [ ] 左侧导航 5 个页面全部功能与本设计一致;顶栏四元素(状态/下一场/开关/试领)在所有页面常驻
- [ ] 语义色全站统一,无第四种"状态色"混用;品牌紫只用于操作与选中态
- [ ] 执行中:主开关/试领/场次编辑正确禁用;步骤条、实时截图、实时日志三处同步更新
- [ ] 试领一次:自动切到日志页并完整回放全过程(含 5 次重试展示)
- [ ] 失败 / 需人工:状态灯变红并保持,提示条给出原因,点击可跳转该场回放
- [ ] 截图缩略图 Lightbox、定位红框叠加正确
- [ ] 词表 chip 增删、Provider 切换(自动带出 base_url/model)、API Key 遮蔽与显示
- [ ] "测试识别"对真实截图返回 JSON 与按钮框叠加,错误有明显提示
- [ ] 上手检查三步按真实状态打勾(绿实心勾 + "已完成"),全部完成后自动收起,按钮可跳转对应页
- [ ] 断网/后端未启动时前端有明确错误态,左下连接灯转黄
- [ ] 配置修改即存即生效,出错有 Toast 提示
- [ ] 空状态(无场次/无记录/无日志)均有引导或占位文案,无空白页
- [ ] 动效:开场/切页/状态变化流畅且不引起布局跳动;`?motion=0` 与系统"减少动态效果"下自动静态呈现
- [ ] 无障碍:键盘可达、焦点环可见;图标全部为内联 SVG(不使用 emoji 作为图标)
